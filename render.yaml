services:
  - type: web
    name: jorge-realty-ai
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bots.lead_bot.main:app --host 0.0.0.0 --port 8001
    healthCheckPath: /health
    envVars:
      - fromGroup: jorge-env
