# Dashboard-specific dependencies
# Install in addition to requirements.txt for Streamlit dashboard containers:
#   pip install -r requirements.txt -r requirements-dashboard.txt

streamlit==1.57.0
pandas==3.0.3
numpy==2.4.6
